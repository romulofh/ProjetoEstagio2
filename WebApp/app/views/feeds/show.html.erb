<p id="notice"><%= notice %></p>

<p>
  <strong>feed:</strong>
  <div>  <%= @feed.source_name %> </div>

  <div>  <%= @feed.category_name %> </div>

  <div>  <%= @feed.url %> </div>
</p>

<%= link_to 'Edit', edit_feed_path(@feed) %> |
<%= link_to 'Back', feeds_path %>
